import{animateTimerProgressBar,stopTimerProgressBar}from"../utils/dom/domUtils.js";import globalState from"../globalState.js";export const getTimerLeft=()=>globalState.timeout&&globalState.timeout.getTimerLeft();export const stopTimer=()=>{if(globalState.timeout)return stopTimerProgressBar(),globalState.timeout.stop()};export const resumeTimer=()=>{if(globalState.timeout){const t=globalState.timeout.start();return animateTimerProgressBar(t),t}};export const toggleTimer=()=>{const t=globalState.timeout;return t&&(t.running?stopTimer():resumeTimer())};export const increaseTimer=t=>{if(globalState.timeout){const e=globalState.timeout.increase(t);return animateTimerProgressBar(e,!0),e}};export const isTimerRunning=()=>globalState.timeout&&globalState.timeout.isRunning();